# Production Environment Variables - Fill in real values from Render.com/Cloudinary/etc. Do NOT commit this file!

# Database (Render.com PostgreSQL - copy from dashboard)
DATABASE_URL=postgresql://your_prod_user:your_prod_password@your_prod_host.render.com/your_prod_db_name

# Cloudinary (same as dev, or production-specific if needed)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# NextAuth (for production - generate a strong secret with openssl rand -base64 32)
NEXTAUTH_URL=https://your-render-domain.com  # Your Render app URL
NEXTAUTH_SECRET=your_strong_secret_here

# Other vars (add as needed, e.g., for emails/notifications)
NODE_ENV=production
